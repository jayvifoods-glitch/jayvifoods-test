JAYVI FOODS — V32.1 WEBSITE RELEASE (V32.x stabilization + Admin completion)

Same v31.0 visual design. This release fixes a batch of storefront bugs,
completes catalogue sync between Admin and storefront, fixes an
Admin-session-in-storefront security/UX issue, and adds a full customer
review workflow. Full changelog in DEPLOY.md.

BEFORE DEPLOYING:
1. Fill in supabase-config.js if not already done (SUPABASE_URL,
   SUPABASE_ANON_KEY) — unchanged from V32.0.
2. Run supabase_migration_reviews_v32_1.sql in SQL Editor — this is a
   NEW migration, additive to supabase_schema_phase1_v3.sql. Run it
   once, after Phase 1, before deploying this release.
3. Review and run cleanup_test_orders.sql yourself (I cannot execute
   this — no live database connection). Report back the order numbers
   it found before/after, per the instructions in that file.
4. Read DEPLOY.md in full — it has the complete changelog and a
   verification checklist for both the storefront and Admin.

UPLOAD RULE:
Replace the complete repository contents with this package. Do not mix
older v15-v32.0 files. Keep the entire images/ folder.

GitHub Pages:
- Branch: main
- Folder: / (root)
